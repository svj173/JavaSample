

Старт демона
/usr/sbin/slapd -h ldap:/// ldapi:/// -g openldap -u openldap -F /etc/ldap/slapd.d

Расположение - /usr/shar/slapd

/var/run/slapd/slapd.args

module - /usr/lib/ldap

base of you directory in database  -
suffix      dc=ez-ldap,dc=loc

rootdn      cp=admin,dc=ez-ldap,dc=loc



245 server
admin / admin

